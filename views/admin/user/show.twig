{% extends "partials/layout-admin.twig" %}

{% block content %}
    <h1 class="page-header">
        Utilisateur : {{ user.username }}
        <div class="float-right">
            <a href="{{ path_for('admin_user_list') }}" class="btn btn-secondary">< Retour</a>
            <a href="{{ path_for('admin_user_edit', {id: user.id}) }}" class="btn btn-primary">Modifer</a>
        </div>
    </h1>

    <div class="card">
        <div class="card-body">
            <p>Nom d'utilisateur : <b>{{ user.username }}</b></p>
            <p>Adresse email : <b>{{ user.email }}</b></p>
        </div>
    </div>
{% endblock %}
